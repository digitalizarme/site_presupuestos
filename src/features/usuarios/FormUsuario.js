import React, { Component } from 'react';

export default class FormUsuario extends Component {
  static propTypes = {

  };

  render() {
    return (
      <div className="usuarios-form-usuario">
        Component content: usuarios/FormUsuarios
      </div>
    );
  }
}
